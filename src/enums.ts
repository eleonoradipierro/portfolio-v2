export enum SETTINGS_OPTION {
  preset = "presets",
  custom = "custom",
}

export enum NAVBAR_OPTIONS {
  about = "about",
  skills = "skills",
  projects = "projects",
  experiences = "experiences",
}
