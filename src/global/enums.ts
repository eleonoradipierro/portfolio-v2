export enum SettingsOption {
  PRESETS = "presets",
  CUSTOM = "custom",
}

export enum NavBarOption {
  ABOUT = "about",
  SKILLS = "skills",
  PROJECTS = "projects",
  EXPERIENCES = "experiences",
}
